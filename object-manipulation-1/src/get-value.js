/* exported getValue */
// getValue
// input: Object and key name
// output: value of key property in Object

// PSEUDOCODE
// return Object's key property's value

function getValue(object, key) {
  return object[key];
}
